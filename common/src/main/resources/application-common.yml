logging:
  level:
    com.suhas: DEBUG
    org.springframework.cloud.gateway: ERROR
springdoc:
  show-actuator: true
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
jwt:
  secret: ${JWT_SECRET}